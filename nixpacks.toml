[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r portal_wps_backend/requirements.txt"
]

[start]
cmd = "python3 portal_wps_backend/src/main.py"
