{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd portal_wps_backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd portal_wps_backend && python src/main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
